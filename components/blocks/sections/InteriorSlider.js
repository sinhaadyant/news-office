import InteriroSlider from "@/components/slider/InteriroSlider";

export default function InteriorSlider({ blockData }) {
  return (
    <section className="slider__area fix">
      <div className="container">
        <InteriroSlider />
      </div>
    </section>
  );
}
